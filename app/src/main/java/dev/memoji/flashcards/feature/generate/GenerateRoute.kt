package dev.memoji.flashcards.feature.generate

/** Where the Add Cards flow lives in the graph. It takes no arguments — it starts empty. */
object GenerateRoute {

    const val PATTERN = "generate"
}
