<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Backup stays off until the ticket that stores the API key can turn it on together with
      the rules that exclude the key from it. ADR 0002 requires Decks and Cards to be backed
      up and the credential not to be; enabling backup before those rules exist would ship
      the leak first and the rule second. allowBackup covers Android 11 and below,
      dataExtractionRules covers 12 and above.
    -->
    <application
        android:name=".FlashcardsApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flashcards">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Flashcards">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
